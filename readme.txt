### math 287 project ###
There are two parts in the code
1. Numerical experiment: run the R code (Yang_Xu_code.R) directly, I remove the install.packages(), so you may install some packages if needed.
2. Application: the experiment of handwritten digits. Two python code. import model.py first then run the result.py.